# cabeca_cauda.ex

# Definindo uma lista
minha_lista = [1, 2, 3, 4, 5]

# Obtendo a cabeça (primeiro elemento)
cabeça = hd(minha_lista)

# Obtendo a cauda (restante da lista)
cauda = tl(minha_lista)

# Imprimindo os resultados
IO.puts "Cabeça da lista: #{cabeça}"
IO.puts "Cauda da lista: #{cauda}"
