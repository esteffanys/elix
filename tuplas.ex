# tupla.ex

# Criando uma tupla representando coordenadas
coordenadas = {:latitude, :longitude}

# Atribuindo valores à tupla
coordenadas_com_valores = {:latitude, 40.7128, :longitude, -74.0060}

# Imprimindo a tupla resultante
IO.inspect(coordenadas_com_valores)
